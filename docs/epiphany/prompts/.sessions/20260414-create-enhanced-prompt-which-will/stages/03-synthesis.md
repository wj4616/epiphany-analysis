<prompt>
  <meta source="epiphany-prompt"/>

  <role>
    You are an expert specification author for modular Claude Code agent skills. You have deep familiarity with the prompt-epiphany skill (ideation → synthesis → verification pattern), the epiphany-genius skill (modular subagent orchestration, 10-section report shape), and the brainstorming skill's expected input contract. You are writing a detailed specification for a new skill provisionally named epiphany-analysis. You are NOT implementing the skill, you are NOT writing code, and you are NOT executing the pipeline described below — you are producing a detailed specification document that a human developer plus a future implementation agent can use to build the skill.
  </role>

  <context>
    <consumer>
      The consumer of this prompt is the brainstorming skill. The brainstorming skill's output is a DETAILED SPECIFICATION (not code, not the finished skill). The target implementation environment is the Claude Code AI agent system. The spec's downstream reader is a human developer plus a future implementation agent.
    </consumer>

    <background>
      <origin_statement>
        we developed this tree of thought prompt model for ai system to follow the thought and reasoning process of comparing one input, to an analysis of that input generated with epiphany-genius skill which makes a report using advanced cognitive techniques to generate the analysis, or any other method, any analysis of any input should be able to use this skill to drastically improve their original input. create the ideal skill.
      </origin_statement>
      <provenance>
        prompt-epiphany skill is proven to use ideation, synthesis, and verification to enhance an input. now we can create an even better output be utilizing an analyis, taking elements of prompt-epiphany design
      </provenance>
      <design_note>
        We also want this skill to run programatically and be modular in design, reeady to easily integrate with the modular system in epiphany-genius in future. allowing for easier re-engineering, and for steps to be followed in a precise and optimal manner.
      </design_note>
      <outline_disclaimer>
        the node pipeline is just a rough outline, we need to enhance this process into full tree of thought or graph of thought to create the ultimate analysis and refinement ai agent skill tool
      </outline_disclaimer>
      <sibling_skills>
        Known background — do not restate their internals:
        - prompt-epiphany / prompt-epiphany skill — source of the ideation / synthesis / verification loop pattern.
        - epiphany-genius / epiphany-genius skill — produces a 10-section analysis report via modular subagent orchestration; the analysis that epiphany-analysis is primarily designed to consume.
        - brainstorming skill — the downstream consumer of THIS prompt; produces the detailed spec for epiphany-analysis.
      </sibling_skills>
    </background>

    <term_anchors>
      Use these terms consistently throughout the produced spec. Do not paraphrase the node labels.
      - node a — the input. "node a is the input. it could be specified directly in the user prompt context, or be stored in a file or other format provided by the user."
      - node b — the analysis document.
      - node a1 — analysis of node a. ("analysis of node a goes in node a1 and analysis of node b goes in node b1")
      - node b1 — analysis of node b.
      - node C — unfiltered idea pool. "create idea storage node C. node C stores each idea generated by previous step analysis in an ai optimized database."
      - node D — accepted ideas after pro/con filter ("create idea storage node d"; ideas we actually want to use).
      - node E — solutions store; two solutions generated per accepted idea, best one passed.
      - x2 node — parallel per-section naming that appears in the input; explicitly flagged as potentially equivalent to node C/D/E. To be documented in the spec as an open question (see &lt;edge_cases&gt; and &lt;verification&gt;).
      - node x3 — parallel per-section naming paired with x2; same ambiguity flag.
      - "10 main sections" — the section structure of an epiphany-genius report; to be enumerated by reading the reference URL in &lt;references&gt;.
      - "ideation, synthesis, and verification" — the prompt-epiphany loop pattern referenced throughout.
      - "tree of thought or graph of thought" — the target pipeline topology.
      - "modular flow optimized for claude code ai agent system" — the implementation target.
      - "ready to easily integrate with the modular system in epiphany-genius" — forward-compatibility requirement.
      - "ai optimized database" — the storage-language phrase used in the original input for Node C / Node D / Node E; treat as structured in-context / stage-file storage unless the spec proposes otherwise.
      - epiphany-analysis — the skill being specified.
      - claude code / claude code ai agent system — the target runtime environment.
    </term_anchors>

    <user_meta_goal>
      create an enhanced prompt which will work for input to brainstorming skill to write detailed spec from:
      make a skill which takes an analysis of an input, thinks about how the analysis can be used to improve the input, and makes changes creating a new improved version
      the goal is to create an improved version of the original input using the analysis
    </user_meta_goal>
  </context>

  <task>
    Produce a detailed specification for a Claude Code modular agent skill (provisionally named epiphany-analysis) that:
    - takes an analysis of an input, thinks about how the analysis can be used to improve the input, and makes changes creating a new improved version.

    Before writing the spec, reason through the full pipeline step-by-step, node-by-node. For EACH node (A, B, A1, B1, C, D, E), the spec MUST describe:
    1. Purpose — what the node represents.
    2. Inputs — what feeds into the node.
    3. Outputs — what the node produces.
    4. Storage schema — how data is held (structured in-context YAML / stage file / other). Node C, Node D, and Node E are described in the source input as "ai optimized database" / "improvement storage node" / "solutions store" — the spec must pick a concrete representation for a Claude Code skill (most plausibly structured in-context / stage-file storage), not a literal persistent database.
    5. Termination condition — every iterate/run-until rule MUST be bounded.
    6. Integration points — how the node plugs into the epiphany-genius modular system for future integration.

    Explicitly frame the phases:
    - Nodes A, B, A1, B1 — analysis phase. "how does node b's content's meaning after full analysis which is in a1 provide insight to issues, or potential problems, bugs which can be fixed or improvements which can be made to input in node a"
    - Node C — unfiltered idea pool (ideation).
    - Node D — accepted-ideas filter (pro/con/utility decision).
    - Node E — two-solutions-compared-and-best-chosen store.

    The spec must mandate ideation / synthesis / verification loops modeled on prompt-epiphany at both (a) the per-idea solution-engineering level and (b) the final-integration level.

    Then produce the spec in the shape described in &lt;output_format&gt;, self-audit it per &lt;verification&gt;, and emit it.
  </task>

  <pipeline>
    <phase id="0" name="full-detail analysis">
      phase 0: analyze both nodes in full, taking into account every details concept idea facet large small or fine, including technical details, do not generalize or glaze over details, all details must be reviewed for both node a and node b and overall goal described
    </phase>

    <node id="A">
      <purpose>Original user input. Source of truth for the content being improved.</purpose>
      <directive>store original context provided by user into node A</directive>
      <source_rule>node a is the input. it could be specified directly in the user prompt context, or be stored in a file or other format provided by the user.</source_rule>
    </node>

    <node id="B">
      <purpose>The analysis document of the input. Primary driver of improvement ideas.</purpose>
      <reference_form>this file is an example of an analysis created by epiphany-genius skill. this is the main form of analysis that our new epiphany-analysis should target (see &lt;references&gt;).</reference_form>
    </node>

    <node id="A1">
      <purpose>Full analysis of node A.</purpose>
      <directive>analysis of node a goes in node a1 and analysis of node b goes in node b1</directive>
    </node>

    <node id="B1">
      <purpose>Full analysis of node B.</purpose>
      <directive>analysis of node a goes in node a1 and analysis of node b goes in node b1</directive>
    </node>

    <node id="C">
      <purpose>Unfiltered idea pool. Every generated improvement candidate is stored here as an individual element.</purpose>
      <directives>
        - create and synthesize ideas and pass each idea as an indivudal element stored in node C
        - create idea storage node C. node C stores each idea generated by previous step analysis in an ai optimized database.
        - generate new ideas for improvements based on node b analysis, to make a better spec node a, storing in database
        - iteratively run phase a, passing all ideas generated into storage node c
        - run until out of ideas completely regarding how to improve or solve issues based on the context, storing all in node C database
        - each idea stored in database,
      </directives>
      <creativity_rule>be creative and analyze from new perspectives with different roles and goals.</creativity_rule>
      <storage_hint>all databases in system expand and create relationships as neccessary for ideal design</storage_hint>
    </node>

    <node id="D">
      <purpose>Accepted-ideas store. Ideas from C that pass the pro/con/utility filter.</purpose>
      <directives>
        - now that we have thought and stored the ideas in node C, they must be analyzed
        - iterate over each idea we generated which is held in node C database data.
        - create idea storage node d
        - decide if we should use the idea from node C to improve the node A input when creating the final improved version output from this skill
        - either accept the idea if adequate and store in node D improvement storage node which holds the ideas we actually want to use or discard ideas we don't want to use
        - iterate over every idea in node C, be sure we don't miss anything
        - each idea in database analyzed for pro/con/utlity/etc and decide if we use the idea
      </directives>
      <judgment_criteria>considering overall utility and relevancy, and if it will create a better product without excessive overengineering or complexity, or resolves any potential issues, we want the best product features that are usable for humans, must be realistic to implement without overcomplexity or regression</judgment_criteria>
      <creativity_rule>be creative and analyze from new perspectives with different roles and goals.</creativity_rule>
    </node>

    <node id="E">
      <purpose>Solutions store. For each accepted idea in D, two solutions are generated, compared, and only the best one is stored here.</purpose>
      <directives>
        - When all analyzed, analyze the first improvement node D. compare to spec in node A, and engineer the optimal solution to achieve the improvement or resolution we are working on. pass this solution into node E which stores our solutions.
        - continue analyzing each improvement listed in node d database data. develop the solution for all improvements, storing the solution individually in node E
        - generate two solutions seperately.
        - analyze solutions compare pro/con and utility and over-engineering limited
        - pass only best solution
        - we want to use a process of ideation, synthesis, and verification for each individual idea. referene the process seen in prompt-epiphany skill
        - continue until all improvements in improvement storage node are fully resolved with solutions stored in node E
      </directives>
    </node>

    <integration_phase>
      <directives>
        - create a final version of the input originally held in node A which flushly integrates all solutions held in node E, enhancing the original based on the analysis verified for accuracy and completeness
        - we want to use a process of ideation, synthesis, and verification
        - use x3 iterating ofver all solutions, integrating all to to resolve all items integrating to final enhanced version taking the solutions and creating a final enhanced version of solution,
      </directives>
    </integration_phase>

    <section_pipeline name="per-section x2/x3 restatement">
      <!--
        The input contains a parallel restatement of the pipeline using node names "x2" and "x3".
        This block is reproduced here verbatim. Its relationship to Nodes C/D/E is an EXPLICIT
        open question: whether it is (a) the same pipeline described twice for emphasis, (b) a
        nested sub-pipeline that runs per-section of the 10-section analysis (most plausible
        reading, since these bullets appear inside the 10-section-handling discussion), or (c)
        a second parallel design the user wants merged. The spec MUST document this ambiguity
        rather than silently picking one reading — see &lt;verification&gt; and &lt;edge_cases&gt;.
      -->
      <directives>
        - each idea stored in database,
        - each idea in database analyzed for pro/con/utlity/etc and decide if we use the idea
        - if usable, store in new  x2 node
        - iterate the items in x2 node and develop the solution to implement improvements or enhancements or resolve issues, storing solution , to node x3
        - use x3 iterating ofver all solutions, integrating all to to resolve all items integrating to final enhanced version taking the solutions and creating a final enhanced version of solution,
      </directives>
    </section_pipeline>
  </pipeline>

  <section_tailoring>
    <requirement>
      look at the analysis, there are 10 main sections. we want each section to get an independent analysis optimized for that specific section. we can't let this process take too long though. but we can tailor the analysis to each step.
    </requirement>
    <requirement>
      look at each of the ten sections and decide how to make this skill have analysis which cater for that particular section's designed focus
    </requirement>

    <required_output>
      The spec MUST produce a section-tailoring map. For each of the epiphany-genius report's 10 main sections, the map has one row declaring:
      - section name
      - section-specific focus
      - idea-extraction strategy
      - special handling rule (if any)

      If the 10 sections are not already known to the spec author, the spec must instruct the resulting skill to read the reference URL in &lt;references&gt; at runtime to enumerate them. If the reference cannot be read, see &lt;verification&gt; escape hatches.
    </required_output>

    <pre_seeded_special_handling>
      <rule target="headline insight / primary conclusion">
        the headline insight or primary conclusion is probably a main indicator of an area to check for primary concern to base enhancement off of initially, before moving to all other portions of analysis
      </rule>
      <rule target="theory collisions">
        in theory collisions, for each collision claim a and claim b should be analyzed and compared, take into account the discrimating condition, and decide what it all means, and how to create an enhanced solution based on the findings
      </rule>
      <rule target="alternative hypotheses">
        not all aspects of analysis are useful, for instance there is alternative hypothesis section, but most of those hypothesis are not correct, only one version will be correct
      </rule>
    </pre_seeded_special_handling>
  </section_tailoring>

  <constraints>
    <do>
      - DO preserve every node label (A, B, A1, B1, C, D, E) verbatim in the spec — no paraphrasing ("the idea store" instead of "node C" is forbidden).
      - DO preserve the reference analysis URL verbatim: file:///home/myuser/docs/epiphany/genius/20260414-prompt-cog-skill-design-spec/report.md
      - DO specify ideation / synthesis / verification loops modeled on prompt-epiphany at both the per-idea solution-engineering step and the final-integration step.
      - DO mandate the "generate two solutions separately, compare pro/con and utility, over-engineering limited, pass only best solution" rule at the Node D → Node E solution-engineering step.
      - DO specify an epiphany-genius detection check (including version-drift tolerance) AND a fallback path for non-genius analyses.
      - DO mandate a fresh separate final-output file that does NOT overwrite the original spec — "create a new seperate final specification integrating all solutions from node E, do not overwrite existing specification with the changes, save a fresh copy of the full enhanced output only".
      - DO design the skill as a modular flow optimized for claude code ai agent system, ready to easily integrate with the modular system in epiphany-genius.
      - DO mandate full verification and validation as the final step before output, "as seen in other skills like prompt-epiphany".
      - DO require the skill to "verify all changes integrated flush with self-audit focusing on being bug-free, no errors issues or potential problems".
      - DO allow "do research or ask questions as neccessary" behavior at runtime — but the spec must clarify whether this applies to (a) the spec-writer while writing the spec, (b) the resulting skill at runtime, or (both). Most plausibly (b); spec should resolve explicitly.
      - DO treat "run until out of ideas completely" as "until a pass generates no new ideas" rather than unbounded looping.
      - DO cover every INVENTORY item — every embedded directive, every iteration rule, every verification rule, every edge case — no summarization, no dropping.
    </do>

    <do_not>
      - DO NOT remove, paraphrase, or summarize any item listed in 01-inventory.md — every item appears in the produced spec in substance.
      - DO NOT introduce infinite loops — every iterate / run-until rule has a termination condition (see &lt;termination_and_budget&gt;).
      - DO NOT overengineer. "must be realistic to implement without overcomplexity or regression". "we want the best product features that are usable for humans".
      - DO NOT produce a spec that would cause regression or loss of information from Node A during integration.
      - DO NOT silently merge the Nodes A–E pipeline with the parallel x2/x3 restatement. Document their relationship explicitly (equivalent, nested per-section sub-pipeline, or ambiguous) — most plausible reading is a per-section sub-pipeline, but leave as a documented open question if evidence is insufficient.
      - DO NOT overwrite the original specification. Fresh copy only.
      - DO NOT assume a literal persistent database; "ai optimized database" is interpreted as structured in-context / stage-file storage unless the spec author proposes a concrete alternative.
    </do_not>

    <termination_and_budget>
      The spec MUST define:
      (a) Per-node max-iteration caps (e.g., max ideas per Node C pass, max passes before Node C is considered exhausted).
      (b) A skill-global max-total-pass cap that binds overall runtime.
      (c) A rule making explicit that "run until out of ideas completely regarding how to improve or solve issues based on the context, storing all in node C database" means "until a full ideation pass generates zero new unique ideas" rather than unbounded.
      (d) Soft time / token budget guidance (the spec picks a sensible default — no specific number is mandated by this prompt, but one MUST be declared in the resulting spec).
      This binds the loose "can't let this process take too long" directive to a concrete spec requirement without inventing numbers the user did not provide.
    </termination_and_budget>
  </constraints>

  <priority_rules>
    Use these tiebreakers to resolve conflicts when drafting the spec. Higher-numbered rules do NOT override lower-numbered rules — each addresses a distinct conflict.

    1. Completeness vs. process-time budget — "run until out of ideas completely" vs. "we can't let this process take too long": prefer completeness bounded by the declared max-iteration caps in &lt;termination_and_budget&gt;. Ideation stops when a pass produces no new unique ideas OR the declared cap is hit, whichever is first.

    2. Creative exploration vs. realistic-to-implement — "be creative and analyze from new perspectives with different roles and goals" vs. "must be realistic to implement without overcomplexity or regression": prefer realistic-to-implement. Creative ideation is free at Node C; Node D MUST reject any idea that is not realistic-to-implement, would cause regression, or would overengineer the result.

    3. epiphany-genius-tailored flow vs. generic flow — if Node B is detected as an epiphany-genius report (current version OR near-future version with drift), use the 10-section tailored flow; otherwise fall back to the generic full-document flow (read all of Node B, extract improvement candidates across the whole document).

    4. Novelty vs. information preservation — if an idea would cause regression or loss of information from Node A, discard it regardless of novelty.
  </priority_rules>

  <edge_cases>
    <case id="a" name="epiphany-genius current version">
      Analysis is an epiphany-genius report, exact current version → run full 10-section tailored flow.
    </case>
    <case id="b" name="epiphany-genius version drift">
      Analysis is epiphany-genius-like but version-drifted (extra or renamed sections, new fields). Perform a "specific check if the analysis is the product of any version of epiphany-genius skill (even if slightly different due to changes made in new version of skill)". Detection via structural heuristic (presence of sections such as headline insight, theory collisions, alternative hypotheses). Run tailored flow with best-effort section mapping.
    </case>
    <case id="c" name="non-epiphany-genius analysis">
      Analysis is not epiphany-genius. Fallback — "if it is not , we still want to be able to perform the improvement to the original input". Run generic full-document flow: full read of Node B, extract improvement candidates across the whole document.
    </case>
    <case id="d" name="theory collisions handling">
      Theory collisions section: for each collision, compare claim A vs claim B, account for the discriminating condition, decide what it all means, and emit a resolved finding used to create an enhanced solution.
    </case>
    <case id="e" name="alternative hypotheses low-signal">
      Alternative hypotheses section: treat as low-signal. Only the hypothesis that best fits the discriminating evidence is used; others are discarded. "most of those hypothesis are not correct, only one version will be correct".
    </case>
    <case id="f" name="headline insight primary anchor">
      Headline insight / primary conclusion: "probably a main indicator of an area to check for primary concern to base enhancement off of initially, before moving to all other portions of analysis". Process first.
    </case>
    <case id="g" name="Node A source is a file">
      "node a is the input. it could be specified directly in the user prompt context, or be stored in a file or other format provided by the user." → If Node A source is a file path or other non-inline format, read its contents as Node A.
    </case>
    <case id="h" name="zero-ideas section">
      Ideation produces zero ideas for a section → mark section complete and move on. Do NOT retry indefinitely.
    </case>
    <case id="i" name="x2/x3 parallel pipeline relation to C/D/E">
      The x2/x3 restatement is documented as an explicit open question. Most plausible reading: x2 = per-section Node C/D filter layer, x3 = per-section Node E solutions layer. The spec MUST either:
      (i) declare them equivalent to Node C/D/E (with evidence), or
      (ii) describe them as a nested per-section sub-pipeline invoked once per one of the 10 sections, or
      (iii) emit both structures as a documented ambiguity requiring user resolution.
      See &lt;verification&gt; escape hatches.
    </case>
    <case id="j" name="research/ask-questions scoping">
      "do research or ask questions as neccessary" — the spec MUST explicitly scope this: whether it applies to (a) the spec-writer while writing the spec, (b) the resulting epiphany-analysis skill at runtime, or (both). Most plausible reading: (b). Do not silently pick a scope.
    </case>
  </edge_cases>

  <references>
    <!--
      Single source of truth for precision-critical external identifiers.
      These MUST appear verbatim in the produced spec. Do not truncate, re-scheme, or paraphrase.
    -->

    <reference_analysis>
      <url>file:///home/myuser/docs/epiphany/genius/20260414-prompt-cog-skill-design-spec/report.md</url>
      <role>Canonical example of the main form of analysis that epiphany-analysis targets.</role>
      <original_directive>this file is an example of an analysis created by epiphany-genius skill. this is the main form of analysis that our new epiphany-analysis should target, and should have a specific check if the analysis is the product of any version of epiphany-genius skill (even if slightly different due to changes made in new version of skill) file:///home/myuser/docs/epiphany/genius/20260414-prompt-cog-skill-design-spec/report.md if it is not , we still want to be able to perform the improvement to the original input</original_directive>
    </reference_analysis>

    <sibling_skills>
      - prompt-epiphany / prompt-epiphany skill — "we want to use a process of ideation, synthesis, and verification for each individual idea. referene the process seen in prompt-epiphany skill"; "final step before output is full verification and validation as seen in other skills like prompt-epiphany".
      - epiphany-genius / epiphany-genius skill — "ready to easily integrate with the modular system in epiphany-genius in future".
      - brainstorming skill — downstream consumer of this prompt.
      - claude code / claude code ai agent system — target runtime environment.
    </sibling_skills>
  </references>

  <verification>
    <self_audit_checklist>
      Before emitting the spec, the brainstorming skill MUST self-audit against this checklist. If any check fails, revise the spec and re-run the checklist until pass.

      1. Every item in 01-inventory.md appears in the spec, unchanged in substance.
      2. Every node (A, B, A1, B1, C, D, E) has: purpose, inputs, outputs, storage schema, termination condition, and integration points defined.
      3. Every iteration rule from the source input has an explicit termination condition in the spec (caps declared in &lt;termination_and_budget&gt; are honored).
      4. Both paths are specified: (a) epiphany-genius detection (including version drift) AND (b) the generic fallback for non-genius analyses.
      5. The "generate two solutions separately, compare pro/con and utility, over-engineering limited, pass only best solution" rule appears at the Node D → Node E solution-engineering step.
      6. The fresh-copy output rule appears in the spec's output-file section: "create a new seperate final specification integrating all solutions from node E, do not overwrite existing specification with the changes, save a fresh copy of the full enhanced output only".
      7. No DO NOT / anti-pattern from &lt;constraints&gt; is violated by any spec section.
      8. Node labels A, B, A1, B1, C, D, E appear verbatim; the reference URL appears verbatim.
      9. Ideation / synthesis / verification loops modeled on prompt-epiphany are specified at BOTH (a) per-idea solution-engineering and (b) final-integration.
      10. The full self-audit rule is carried forward in the produced spec: "verify all changes integrated flush with self-audit focusing on being bug-free, no errors issues or potential problems".
      11. "analysis verified for accuracy and completeness" is honored as a final-integration precondition.
      12. The x2/x3 relation to C/D/E is explicitly documented (equivalent, nested sub-pipeline, or flagged ambiguity) — NOT silently merged.
      13. The "research / ask questions" directive is explicitly scoped (spec-writer-time, runtime, or both).
    </self_audit_checklist>

    <escape_hatches>
      - If the reference analysis file at the embedded URL cannot be read while writing the spec, state explicitly that the 10-section enumeration must be inferred from the report content by the resulting skill at runtime, and document the unknown as a spec gap rather than guessing a list.
      - If the parallel x2/x3 pipeline cannot be conclusively resolved as equivalent to Nodes C/D/E, emit the spec with both structures documented and flag the ambiguity as an explicit open question for the user.
      - If a requested behavior cannot be specified without violating the no-overengineering constraint, state the tradeoff in the spec and recommend the simpler option.
      - If any other input-level ambiguity is encountered, prefer documenting it as a spec-level open question over silently resolving it.
    </escape_hatches>
  </verification>

  <output_format>
    The brainstorming skill MUST produce a detailed specification document (not code, not a stub, not the finished skill). The spec has at minimum the following numbered sections. Section headings are required; internal shape of each section is at the brainstorming skill's discretion.

    1. Skill identity and activation — name (epiphany-analysis), one-line description, activation triggers, inputs (Node A, Node B), primary outputs.
    2. Module inventory — one module per major pipeline stage (analysis, ideation, filter, solution-engineering, integration, verification). Each module gets: purpose, inputs, outputs, invocation protocol, and forward-integration notes for the epiphany-genius modular system.
    3. Node data model — one subsection per node (A, B, A1, B1, C, D, E). Each subsection declares purpose, inputs, outputs, storage schema (structured in-context / stage-file / other), termination condition, and integration point.
    4. Section-tailored analysis map — one row per epiphany-genius report section (10 rows when known), with columns (section name, section-specific focus, idea-extraction strategy, special handling rule). Pre-seeded rules from &lt;section_tailoring&gt; MUST be included verbatim.
    5. Ideation / synthesis / verification loops — the prompt-epiphany-modeled loop pattern, applied at (a) per-idea solution engineering and (b) final integration. Describe inputs, outputs, and termination for each loop level.
    6. Two-solutions compare-and-pick rule — applied at Node D → Node E: generate two solutions separately, compare pro/con and utility, limit over-engineering, pass only the best solution.
    7. epiphany-genius version detection and fallback — detection heuristic (including version-drift tolerance), fallback behavior for non-genius analyses.
    8. Self-audit verification block — the final-step verification rules; must carry the &lt;self_audit_checklist&gt; items forward into the spec.
    9. Fresh-copy output file convention — filename strategy, directory location, non-overwrite guarantee. The spec must pick a concrete convention (the original input does not specify location; treat as a required spec-design decision).
    10. Termination conditions and iteration caps — per-node caps, skill-global cap, "until a pass generates no new ideas" rule, soft time/token budget guidance.
    11. Integration points with epiphany-genius modular system — how each module exposes inputs/outputs compatible with that system.
    12. Anti-patterns and DO NOT list — consolidated from &lt;constraints&gt; and any additional anti-patterns discovered while writing the spec.

    Do NOT constrain the spec to a specific markdown template beyond the section list above — internal shape of each section is up to the brainstorming skill's discretion. Markdown is preferred but not mandatory.

    Open questions MUST be surfaced in a final "Open questions" subsection within section 12 (or as an explicit thirteenth section, spec-author's choice). Do not silently resolve them.
  </output_format>
</prompt>
